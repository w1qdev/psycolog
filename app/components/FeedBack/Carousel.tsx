import { motion } from "framer-motion";

const Carousel = () => {
    return (
        <div className="w-full cursor-grab overflow-hidden bg-[#fff]">
            <div className="flex bg-lime-300"></div>
        </div>
    );
};

export default Carousel;
